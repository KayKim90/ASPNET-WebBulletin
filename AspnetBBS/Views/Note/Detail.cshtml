@using Microsoft.AspNetCore.Http;
<div class="row">
    <div class="col-lg-12">
        <h3 class="page-header">Detail contents - @Model.NoteTitle</h3>
    </div>
</div>

<div class="row">
    <div class="col-lg-10">
        <div class="panel-heading">
            <label>Title: </label>@Model.NoteTitle
        </div>
        <div class="panel-body">
            @*A panel in bootstrap is a bordered box with some padding around its content:*@
            @*만약 @Model.NoteContents를 그냥 두면, 태그열로 인식X, 문자열로 인식O*@
            @*그렇기에 밑에처럼 바꿔야 함*@
            @Html.Raw(Model.NoteContents);
        </div>
        <div class="panel-footer text-center">
            <a class="btn btn-primary" asp-controller="Note" asp-action="Index">Go to List</a>
            @*만약 세션과 Model의 UserNo가 같으면 *@
            @if (@Model.UserNo == int.Parse((Context.Session.GetInt32("USER_LOGIN_KEY").ToString())))
            {
                <a class="btn btn-default" asp-controller="Note" asp-action="Edit" asp-route-noteNo="@Model.NoteNo">Edit</a>
                <a class="btn btn-danger delete"
                   data-id="@Model.NoteNo"
                   data-controller="Note"
                   data-action="Delete"
                   data-body-message="Are you sure to delete?">Delete</a>
            }
        </div>
    </div>
</div>